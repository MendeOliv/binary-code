# robots.txt — Código Binário (https://codigobinario.it.ao)
User-agent: *
Allow: /

# API surface is never content: the public API lives on its own host and the
# /api/* path only exists here as a local development proxy.
Disallow: /api/

Sitemap: https://codigobinario.it.ao/sitemap.xml
